import { Vector } from "..";
export declare namespace Maths {
    const randomSeed: (s: number) => () => number;
    const randomRange: (a?: number, b?: number) => number;
    const randomVector: (a?: Vector, b?: Vector) => Vector;
}
//# sourceMappingURL=Random.d.ts.map